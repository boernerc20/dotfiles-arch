# NOTE: hyprpaper doesn't handle symlinks well at startup
# We'll load the wallpaper dynamically via exec-once in hyprland.conf instead
# This config just enables IPC for wal-hypr.sh to work
ipc = on

